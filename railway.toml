[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
# Build context is repo root so Dockerfile can access packages/schemas
buildCommand = ""

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
